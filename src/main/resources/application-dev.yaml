spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/lenceria
    username: postgres
    password: sa
  jpa:
    properties:
      hibernate:
        default_schema: tienda
server:
  port: 8081
logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.Statement: debug
    org.hibernate.orm.connections: debug
